{
  "name": "co-simplify",
  "version": "1.0.0",
  "description": "generator, promise control flow base on co",
  "keywords": [
    "async",
    "flow",
    "generator",
    "coroutine"
  ],
  "devDependencies": {
    "istanbul": "^0.3.2",
    "mocha": "^2.0.0",
    "should": "^4.3.0",
    "thunkify": "^2.1.2"
  },
  "scripts": {
    "test": "mocha --harmony-generators --reporter spec",
    "test-travis": "node --harmony-generators node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha --report lcovonly"
  },
  "files": [
    "index.js"
  ],
  "license": "MIT",
  "repository": "https://github.com/bornkiller/co-simplify",
  "engines": {
    "node": ">= 0.11.13"
  }
}
